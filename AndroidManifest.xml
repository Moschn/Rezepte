<manifest package="ch.simonf.rezepte"
    android:versionCode="1"
    android:versionName="1.0" xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-sdk
        android:minSdkVersion="8"
         android:targetSdkVersion="15"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".activities.MainScreenActivity">
            <intent-filter>
            	<action android:name="android.intent.action.MAIN" />
            	<category android:name="android.intent.category.LAUNCHER" />
        	</intent-filter>
        </activity>
        <activity android:name=".activities.AllProductsActivity"
            android:configChanges="orientation|keyboardHidden"></activity>
        <activity android:name=".activities.Ingredients.EditIngredientActivity"></activity>    
        <activity android:name=".activities.Ingredients.NewIngredientActivity"></activity>
        <activity android:name=".activities.Recipes.AllRecipesActivity"></activity>
        <activity android:name=".activities.Recipes.ViewRecipeActivity"></activity>
        <activity android:name=".activities.Ingredients.AllIngredientsActivity"></activity>
    </application>

</manifest>